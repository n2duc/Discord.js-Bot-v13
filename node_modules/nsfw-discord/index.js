module.exports = require("./src/Nsfw.js");
/*
module.exports = {
    channelCreate: async (client) => {
        let logging = channel.guild.channels.cache.find(
          ch => ch.channelid === `modlogs`
        );
        if (!logging) return;
        let types = {
          text: "Text Channel",
          voice: "Voice Channel",
          category: "Category Channel",
          null: "None"
        };
        let channelCreate = new MessageEmbed()
          .setColor("BLUE")
          .setTitle("...")
          .setDescription([
            `**Name: ${channel.name} (${channel.id}) | ${channel}**`,
            `**Created At: ${channel.createdAt.toLocaleString()}**`,
            `**Type: ${types[channel.type]}**`
          ])
          .setTimestamp();
        logging.send(channelCreate);
      }
    };
    */